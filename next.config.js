/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
   domains : ["source.unsplash.com","i.ibb.co","images.unsplash.com"],
  },
  
};

module.exports = nextConfig;
